- GC segv ... can we ref/unref around argument property fetch? what does
  .property return, exactly?

  try

	require 'vips8'
	op = Vips::Operation.new "black"

	cls = GLib::Type["VipsBlack"].to_class
	prop = cls.property "width"
	=> #<GLib::Param::Int: #<Class:0x0000000280a7f8>#width>

  



- add complex constants, eg. 

	Complex(1, 2)
	=> (1+2i)

- more examples, more realistic examples

- example3.rb segvs around loop 500

  seems to be still related to prop GC

  posted a bug report, need to wait for a fix

  http://sourceforge.net/p/ruby-gnome2/mailman/message/34555949/

  follow up in Nov if no response

